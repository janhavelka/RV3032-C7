# RV3032-C7 2.0.0 implementation report - 2026-07-13

## Scope and evidence policy

This report was created before implementation-source edits and was completed
after the implementation and verification pass. It records both the audited
baseline and the final evidence. Three delegated audits covered lifecycle/jobs,
persistence/integration, and vendor capability semantics; their findings and
focused changes were checked against the source and integrated by the primary
agent.

A second diligent prompt-to-code audit on the same date found and closed
additional transport-layering, lifecycle-evidence, public-contract,
typed-capability, fake-fidelity, primary-phase, example, and HIL-authorization
gaps. The final matrices and verification counts below include that follow-up
rather than the earlier 64-test checkpoint.

A third phase-04.2 audit closed exact deadline-zero and between-instruction
timing defects, completed calendar result evidence, restored the vendor alarm
inactive state, removed an unsupported temperature-threshold ordering policy,
and staged CLKOUT/temperature reconfiguration according to vendor transition
guidance. It also corrected exact offset quantization and public persistence/
electrical contracts.

A fourth phase-04.3 audit anchored EEPROM waits after command-callback
completion, made the minimum generic timeout usable, refreshed queue deadlines
between callbacks, reconciled EEF through direct persistent proof, restored the
queued intended active mirror, retained the first batch failure, and made
partial-read evidence exact. Focused fake tests cover each correction.

A fifth phase-04.4 audit on 2026-07-14 closed primary-cell failure-stage and
attempt-evidence gaps, reserved the callback bound in addition to the 300 ms
cleanup window, stopped normal work after late callbacks, readback-classified
ambiguous active-PMU mutations before cleanup, and anchored the level-switch
settle at exact target-C0 proof. It also removed misleading active-mirror
EEPROM aliases and preserved unnamed lower-six Status flags across the
cooperative clear race. Three read-only delegated audits were integrated by
the primary agent; no hardware access was performed.

Hardware execution, device flashing, physical EEPROM commands, publishing,
commits, tags, and edits to the sibling TunnelMonitor repository were outside
this task. Hardware-in-the-loop status is **NOT RUN**.

## Baseline

- Repository HEAD at start: `8b9e7fb`; declared version: `1.6.0`.
- Native baseline: `python -m platformio test -e native` - 56/56 passed.
- ESP32-S3 baseline: passed; RAM 22,368 bytes, flash 421,086 bytes.
- The baseline already had fixed-capacity job/EEPROM storage, injected
  transport callbacks, tracked/raw wrappers, and health counters.
- The baseline did not meet the requested passive lifecycle or persistence
  contract: lifecycle methods performed policy I/O, OFFLINE suppressed calls,
  ordinary APIs contained synchronous multi-callback work, persistence lacked
  durable direct proof, and primary-cell provisioning was not a dedicated
  bounded semantic operation.

## Vendor interpretation record

The local RV-3032-C7 application manual Rev. 1.3 and datasheet were inspected,
including the prompt's required register, EEPROM, timing, status, password,
PMU, and primary-cell pages.

- Primary-cell target: `(persistentC0 & 0x4C) | 0x20`.
- `TCM=00` disables the charger. `TCR` is preserved because its value is
  electrically inactive while TCM disables charging.
- On application-manual page 141, `TCR=00` is printed in black as the unchanged
  delivery value, while `TCM=00` is red as the required primary-cell policy.
  The driver generalizes that example by preserving an existing inactive TCR
  selection rather than resetting it.
- Status is write-zero-to-clear for PORF/VLF/UF/TF/AF/EVF, while every Status
  write also clears THF/TLF. The fake and typed clear paths model that side
  effect explicitly; guarded clears preserve every unnamed lower-six flag even
  if it asserts between the cooperative read and write.
- EECMD is write-only/read-zero. Active C0-CA mirrors and persistent C0-EA
  EEPROM are independent storage.
- Direct persistence uses `READ_ONE=0x22` and `WRITE_ONE=0x21`; generic
  persistence and primary ensure do not issue `UPDATE_ALL=0x11` or
  `REFRESH_ALL=0x12`.
- Both documented disabled BSM encodings are accepted by the native fake, but
  every driver EECMD path first proves the required BSM00/TCM00 access state.

## Vendor capability matrix

| Functional block | Final public coverage | Execution and persistence contract | Native evidence |
| --- | --- | --- | --- |
| Calendar/time and Unix | `readTime`/`setTime`, Unix/hundredths helpers, status-first snapshot, and verified set/clear reports | Single bursts or budgeted jobs with wrap-safe operation/mutation deadlines; weekday input is normalized for writes | Every reserved calendar mask, BCD/date/weekday failures, invalid-status short circuit, exact `0/1/N` budgets, cutoff crossing, ambiguity and rollover tests |
| Alarm and AIE | Typed time/match/interrupt/flag surfaces, including date-0 comparator deactivation | Active-only cooperative mutation; reads are single bursts; all AE bits set is explicitly every-minute, not disabled | Round-trip, inactive reset state from all-AE-set, every-minute encoding, invalid range, reserved bit, AIE and Status collateral-clear tests |
| Countdown timer and TIE/TF | Typed 12-bit timer plus interrupt/flag surfaces | Budgeted mutation; Timer High reserved bits write zero and are rejected on readback | `0/1/N` budget, 12-bit round-trip, reserved-bit and interrupt tests |
| Periodic update UIE/UF | SECOND/MINUTE frequency, interrupt and flag surfaces | Cooperative two-register update | Exact USEL/UIE bit and readback tests |
| Event input and timestamps | Edge/high-low selection, edge-only or sampled-level qualification, overwrite, ESYN/CLKDE, EIE/EVF, three timestamp sources and reset | Single burst decode; cooperative reset/RMW, including an EVR 0-to-1 transition when EVR remains set | Complete EVI readback, invalid enum, strict/empty timestamp decode, repeated EVI reset and TLow/THigh flag side effects |
| Temperature/events | Single-burst convenience and coherent two-sample reads; independent signed thresholds, signed 1/128 C reference, THF/TLF, events, interrupts and overwrites | Fixed staged job disables detection/interrupts before active-only threshold changes and restores interrupts before detection; C4/C5 reference persistence remains opt-in | Matching/mismatching samples, both threshold orderings, staged write order, signed reference, flags and event fields |
| CLKOUT | Direct NCLKE enable, retained crystal FD and OS/HFD state, interrupt selection and typed CLKF inspection/clear | Fixed staged job rejects active/enabled interrupt CLKOUT, stops direct output, changes C2/C3, then restores NCLKE; optional C0/C2/C3 persistence; >52 MHz electrical caveat documented | Full inactive-field round-trip, stopped-write ordering, 8192 retention, guarded BUSY/no-write, invalid enums, mode-aware getters and CLKF isolation |
| Offset/calibration | Exact signed 0.238418579 ppm measured-error encoding plus independent PORIE/VLIE set/get | Cooperative active C1 update; optional persistence, with reset/refresh durability caveats | Exact -32/+31 endpoints, quantized round-trip, interrupt-bit preservation and out-of-range rejection |
| Backup/trickle | Independent BSM, TCM, TCR and BSIE APIs | Cooperative active update; optional persistence; no lifecycle policy | Field-isolation round-trip and active-only/no-durability test |
| Status/reset/validity | Decoded reads, explicit guarded clears, and distinct hardware EEbusy/EEF inspection/clear | No implicit clear except the named calendar job; stale EEF clear preserves other TEMP_LSB state | Fake W0C/fraction semantics, busy rejection, EEF inspection and THF/TLF collateral guard tests |
| STOP and GP0/GP1 controls | Typed STOP and general-purpose-bit set/get | Active-only cooperative Control 1/2 mutations | Round-trip, STOP prescaler/hundredths effects and transport-failure tests |
| User RAM | Bounded 16-byte read/write | One read burst; write job chunks within budget | Full-range round-trip and zero-I/O range rejection |
| Configuration EEPROM | Typed C0-C5 inspection and opt-in typed persistence | Shared fixed-capacity engine, adaptive two-read proof, compare, one write-one, durable verification, intended-active restoration and cleanup | Read-when-writes-disabled, adaptive sentinel values, ignored active/staging writes, minimum timeout, batch-status and cleanup-evidence tests |
| User EEPROM | Typed 0-31 read/write, maximum 16 bytes per job | Per-byte compare/write-one/direct proof; fixed copied input; stop on first error | Equal-byte skip, changed-byte verification, partial report and no broad command tests |
| Password/protection | Typed active credential load and durable redacted configuration | Password bytes excluded from raw/public result surfaces; protection is disabled before reference changes, the new credential precedes cleanup, and relock follows cleanup/re-enable | Enable/change, wrong credential, committed/noncommitted authentication failures, ordering, relock, redaction and raw-access rejection tests |
| Diagnostics/health | State, counters, last error, settings, raw probe and tracked recover | OFFLINE is observational; transport owner retains retry/recovery policy | Zero-I/O lifecycle, one-callback probe/recover, NACK error mapping and OFFLINE tracked-recover tests |

## Requirement-to-evidence matrix

| Requirement | Final implementation | Completion evidence |
| --- | --- | --- |
| Passive lifecycle | `begin()` validates/binds with zero callbacks; `end()` is zero-I/O and preserves active work; `probe()` is one raw read; `recover()` is one tracked read | Full Config boundary validation, second-begin preservation, ordinary/queued/active-EEPROM end guards, EEPROM-only tick, end/rebegin context, and recover-preserved ensure-latch tests |
| Transport ownership | Library uses only injected callbacks through tracked-to-raw layering; no Wire, bus setup, retry, recovery, logging, heap, or platform delay in core; ensure requires scoped single-attempt reads and writes | Core timing guard, timeout-aware raw-wrapper audit, public callback contract, per-capability failures, callback/physical-attempt counters and bounded owner read-recovery timing |
| Observational health | Tracked wrappers alone update health; OFFLINE does not suppress a valid request | Native OFFLINE-to-READY operation test and failure-code loop |
| Cooperative ordinary work | Existing single job owner handles all ordinary multi-transfer operations; each instruction is at most one callback | Job budget-zero/one/N tests, one-owner budget-one regressions and both embedded builds |
| Composite calendar jobs | Added status-first snapshot and verified calendar write/PORF/VLF-clear jobs with typed result availability | Short circuit, every reserved/BCD/date rejection, output-preserving getters, failed partial results, exact wrap-to-zero, elapsed callback cutoff, `0/1/N` budgets, ambiguity proof and Status side-effect tests |
| Generic persistence | Fixed queue/shared engine now uses safe access, exact pre-mutation capacity, newest-value duplicate coalescing, FIFO, post-callback wait anchors, refreshed time, explicit wait/count/phase/whole bounds, adaptive direct proof after EEF, one write command, intended-active restoration, durable verification and separately evidenced cleanup; ordinary errors remain sticky while cleanup failure cancels later entries | Single/block capacity, full/duplicate/FIFO, zero/one/exact-N budget, elapsed multi-budget cutoff, minimum timeout, cross-engine exclusion, compare/verify, ignored active/staging writes, sticky batch failure, hard-deadline no-callback and cleanup report tests |
| Persistent inspection/user EEPROM | Added fixed-capacity typed C0-C5/user read jobs and copied-input user write job | Adaptive two-read proof including 0x80/0x7F, password-byte rejection, output-preserving/non-consuming getters, proven-byte partial length, read while writes disabled, compare-skip and multi-byte report tests |
| Primary-cell ensure | Sole synchronous multi-callback exception; exact target, lifecycle latch, command-anchored 25/100 ms phases, 1/10 ms waits, write cutoff, callback-plus-cleanup reserve, dispatch-exact evidence, one-write cap, late-return stop, ambiguous-mutation classification, and trusted/untrusted cleanup | Correct/wrong/ambiguous paths, exact command counts, active/queued BUSY matrix, unchanged rejected reports, A-stage attribution, noncommitted READ_ONE error retention, immediate-dispatch cutoff crossing, late EECMD, near-limit ready window, active-PMU no-replay, EEF typing, exact settle anchor, initial-busy and wrap tests |
| Register/status safety | Corrected masks/commands; public direct and staged-RMW allowlists exclude Status/control/password/staging/EECMD/indirect EEPROM; active C0-C5 names cannot be mistaken for persistent access | Reserved-gap/password/staging/indirect single/block rejection, allowed threshold/RAM blocks, RAM-only RMW, between-poll lower-six W0C preservation, reserved-bit and fake W0C tests |
| Native fake fidelity | Fixed-capacity model separates direct, active and persistent state and models POR/daily refresh, initial busy, delayed/ignored commands, EEF, low VDD, TEMP_LSB/Status W0C, write-only/read-zero registers, ambiguous commit and bounded logs | Driver integration tests retain read bytes, callback status/ambiguity, busy/EEF observations, waits, command counts, physical attempts, read recovery and overflow evidence |
| Example safety | Startup performs passive begin then explicit probe; ordinary setters identify active-only behavior; no automatic provisioning or manual EECMD route; ensure prints its complete terminal report | CLI contract guard, S2/S3 example builds, parser self-test, safe dry-run and destructive-authorization rejection |
| Documentation/version | `library.json` is 2.0.0; generated Version.h, changelog, README and maintained docs describe the final contracts | Version generator check, docs source/package guards and package inspection |

## API and migration outcome

Version 2.0.0 intentionally removes `Config::backupMode` and the unsafe
`setPrimaryBatteryBackupDefaults()` helper. It adds the yielding `waitMs`
callback, append-only error codes, typed cooperative results, persistent
inspection/user EEPROM/password APIs, and the explicit
`ensurePrimaryCellConfiguration()` operation. The follow-up adds typed
hundredths, PORIE/VLIE, complete EVI readback, and hardware EEbusy/EEF APIs.
Existing feature names remain
where their hardware capability remains safe, but multi-transfer setters now
return IN_PROGRESS on admission and require `pollJob()`; opt-in persistence is
then advanced through `tick()` or `pollEeprom()`.

## Verification record

| Check | Result |
| --- | --- |
| Native baseline | PASS - 56/56 |
| Native final | PASS - 79 focused scenarios, including retained utility coverage and the protocol-faithful fake |
| ESP32-S3 final | PASS - RAM 22,608/327,680 bytes (6.9%); flash 443,854/1,310,720 bytes (33.9%) |
| ESP32-S2 final | PASS - RAM 37,024/327,680 bytes (11.3%); flash 432,817/1,310,720 bytes (33.0%) |
| Version check | PASS - generated Version.h matches library.json 2.0.0 |
| Core timing guard | PASS |
| CLI contract guard | PASS |
| Documentation source guard | PASS |
| HIL parser self-test | PASS - device-free |
| HIL safe dry-run | PASS - 26 planned functional steps, no serial port opened |
| HIL destructive authorization gate | PASS - missing fresh scope rejected before serial open |
| Diff whitespace check | PASS |
| Package docs/content audit | PASS |
| Physical HIL/flash/EEPROM/voltage/retention | **NOT RUN - explicit authorization required** |

The final embedded builds report PlatformIO's existing multiple-core warning
(6.1.18 active while 6.1.19 is also installed); it did not affect either build.

## TunnelMonitor compatibility audit

No TunnelMonitor file or dependency pin was changed. The library boundary now
supports the planned integration profile: generic persistence can remain
disabled, ordinary work can be serialized by the application's I2C owner with
`pollJob(now, 1, used)`, and the owner may explicitly invoke the bounded primary
ensure once during startup. A TunnelMonitor build and hardware integration were
not run in this repository.

## Intentional omissions

- No physical HIL, upload, EEPROM endurance, voltage/backfeed, power-cycle, or
  retention claim.
- No library-owned bus recovery or replay of a possibly mutating callback.
- No general synchronous multi-callback API beyond primary-cell ensure.
- No update-all/refresh-all use in persistence or primary provisioning.
- No password guessing API, password readback, or unrestricted EECMD surface.
  The phase-04.2-required typed `unlockPasswordProtection(credential)` performs
  one explicit caller-supplied credential load; it does not enumerate, guess,
  retry, retain, or reveal credentials.
- No commit, tag, release publication, or dependency-pin update was made by
  this follow-up audit.

## Worktree state

At the start of the follow-up audit, `HEAD` and `origin/main` were `dcca5e0`
(`Add implementation report for RV3032-C7 version 2.0.0 and introduce
FakeRv3032 test harness`) and the worktree was clean. The follow-up corrections
listed above remain uncommitted. No existing user changes were reset. Temporary
PDF contact sheets and package-audit archives were removed after inspection.
